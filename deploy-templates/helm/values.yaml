replicaCount: 1
image: { repository: ghcr.io/cognis-digital/app, tag: latest, pullPolicy: IfNotPresent }
service: { type: ClusterIP, port: 80, targetPort: 8000 }
resources:
  requests: { cpu: 50m, memory: 64Mi }
  limits: { cpu: 500m, memory: 256Mi }
